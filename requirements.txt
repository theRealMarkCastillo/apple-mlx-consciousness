## Production dependencies (pinned)
# Pinning to latest verified releases as of 2025-11-20.
# Adjust if Apple MLX releases compatibility notes requiring older numpy.

# Core MLX framework
mlx==0.30.0

# Numerical computing
numpy==2.3.5

# Visualization and plotting (used in notebooks)
matplotlib==3.10.7
seaborn==0.13.2

# Data handling (implicit seaborn dependency; listed explicitly)
pandas==2.3.3

## Optional Jupyter stack (uncomment if needed for local notebooks)
# jupyter==1.1.1
# ipykernel==7.1.0

# To install only core runtime (no plotting / notebooks), you could trim to:
# mlx==0.30.0
# numpy==2.3.5
